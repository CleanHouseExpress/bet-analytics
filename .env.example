# Used when running the API directly on the host. Docker Compose overrides this
# with db:5432 and does not publish PostgreSQL to the host.
DATABASE_URL=postgresql+psycopg://bet_analytics:bet_analytics@localhost:5432/bet_analytics
SPORTMONKS_API_TOKEN=your_sportmonks_token
FOOTBALL_DATA_API_TOKEN=your_football_data_token
